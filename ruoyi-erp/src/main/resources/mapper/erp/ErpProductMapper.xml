<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.erp.mapper.ErpProductMapper">
    <resultMap id="ErpProductResult" type="ErpProduct">
        <id property="productId" column="product_id"/>
        <result property="productCode" column="product_code"/>
        <result property="productName" column="product_name"/>
        <result property="categoryName" column="category_name"/>
        <result property="brandName" column="brand_name"/>
        <result property="costPrice" column="cost_price"/>
        <result property="salePrice" column="sale_price"/>
        <result property="status" column="status"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <sql id="selectProductVo">
        select product_id, product_code, product_name, category_name, brand_name, cost_price, sale_price,
               status, create_by, create_time, update_by, update_time, remark
        from erp_product
    </sql>

    <select id="selectProductList" parameterType="ErpProduct" resultMap="ErpProductResult">
        <include refid="selectProductVo"/>
        <where>
            <if test="productCode != null and productCode != ''">and product_code like concat('%', #{productCode}, '%')</if>
            <if test="productName != null and productName != ''">and product_name like concat('%', #{productName}, '%')</if>
            <if test="status != null and status != ''">and status = #{status}</if>
        </where>
        order by product_id desc
    </select>

    <select id="selectProductById" parameterType="Long" resultMap="ErpProductResult">
        <include refid="selectProductVo"/>
        where product_id = #{productId}
    </select>

    <insert id="insertProduct" parameterType="ErpProduct" useGeneratedKeys="true" keyProperty="productId">
        insert into erp_product(product_code, product_name, category_name, brand_name, cost_price, sale_price, status, create_by, create_time, remark)
        values(#{productCode}, #{productName}, #{categoryName}, #{brandName}, #{costPrice}, #{salePrice}, #{status}, #{createBy}, sysdate(), #{remark})
    </insert>

    <update id="updateProduct" parameterType="ErpProduct">
        update erp_product
        <set>
            <if test="productCode != null">product_code = #{productCode},</if>
            <if test="productName != null">product_name = #{productName},</if>
            <if test="categoryName != null">category_name = #{categoryName},</if>
            <if test="brandName != null">brand_name = #{brandName},</if>
            <if test="costPrice != null">cost_price = #{costPrice},</if>
            <if test="salePrice != null">sale_price = #{salePrice},</if>
            <if test="status != null">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            update_time = sysdate()
        </set>
        where product_id = #{productId}
    </update>

    <delete id="deleteProductByIds" parameterType="Long">
        delete from erp_product where product_id in
        <foreach collection="array" item="productId" open="(" separator="," close=")">
            #{productId}
        </foreach>
    </delete>
</mapper>
