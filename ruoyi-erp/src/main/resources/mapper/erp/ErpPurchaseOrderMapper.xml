<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.erp.mapper.ErpPurchaseOrderMapper">
    <resultMap id="ErpPurchaseOrderResult" type="ErpPurchaseOrder">
        <id property="purchaseId" column="purchase_id"/>
        <result property="purchaseNo" column="purchase_no"/>
        <result property="supplierId" column="supplier_id"/>
        <result property="supplierName" column="supplier_name"/>
        <result property="warehouseId" column="warehouse_id"/>
        <result property="totalAmount" column="total_amount"/>
        <result property="purchaseStatus" column="purchase_status"/>
        <result property="expectedTime" column="expected_time"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <sql id="selectPurchaseOrderVo">
        select purchase_id, purchase_no, supplier_id, supplier_name, warehouse_id, total_amount,
               purchase_status, expected_time, create_by, create_time, update_by, update_time, remark
        from erp_purchase_order
    </sql>

    <select id="selectPurchaseOrderList" parameterType="ErpPurchaseOrder" resultMap="ErpPurchaseOrderResult">
        <include refid="selectPurchaseOrderVo"/>
        <where>
            <if test="purchaseNo != null and purchaseNo != ''">and purchase_no like concat('%', #{purchaseNo}, '%')</if>
            <if test="supplierName != null and supplierName != ''">and supplier_name like concat('%', #{supplierName}, '%')</if>
            <if test="purchaseStatus != null and purchaseStatus != ''">and purchase_status = #{purchaseStatus}</if>
        </where>
        order by purchase_id desc
    </select>

    <select id="selectPurchaseOrderById" parameterType="Long" resultMap="ErpPurchaseOrderResult">
        <include refid="selectPurchaseOrderVo"/>
        where purchase_id = #{purchaseId}
    </select>

    <insert id="insertPurchaseOrder" parameterType="ErpPurchaseOrder" useGeneratedKeys="true" keyProperty="purchaseId">
        insert into erp_purchase_order(purchase_no, supplier_id, supplier_name, warehouse_id, total_amount, purchase_status, expected_time, create_by, create_time, remark)
        values(#{purchaseNo}, #{supplierId}, #{supplierName}, #{warehouseId}, #{totalAmount}, #{purchaseStatus}, #{expectedTime}, #{createBy}, sysdate(), #{remark})
    </insert>

    <update id="updatePurchaseOrder" parameterType="ErpPurchaseOrder">
        update erp_purchase_order
        <set>
            <if test="purchaseNo != null">purchase_no = #{purchaseNo},</if>
            <if test="supplierId != null">supplier_id = #{supplierId},</if>
            <if test="supplierName != null">supplier_name = #{supplierName},</if>
            <if test="warehouseId != null">warehouse_id = #{warehouseId},</if>
            <if test="totalAmount != null">total_amount = #{totalAmount},</if>
            <if test="purchaseStatus != null">purchase_status = #{purchaseStatus},</if>
            <if test="expectedTime != null">expected_time = #{expectedTime},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            update_time = sysdate()
        </set>
        where purchase_id = #{purchaseId}
    </update>

    <delete id="deletePurchaseOrderByIds" parameterType="Long">
        delete from erp_purchase_order where purchase_id in
        <foreach collection="array" item="purchaseId" open="(" separator="," close=")">
            #{purchaseId}
        </foreach>
    </delete>
</mapper>
